<p id="notice"><%= notice %></p>

<h1>Список вагонов</h1>

<table>
  <thead>
    <tr>
      <th>Тип вагона</th>
      <th>Верхних мест</th>
      <th>Нижних мест</th>
    </tr>
  </thead>

  <tbody>
    <% @carriages.each do |carriage| %>
      <%= render 'carriage', carriage: carriage %>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Новый вагон', new_carriage_path %>
